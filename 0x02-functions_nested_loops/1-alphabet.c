#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - in lowercase, followed by a new line
 */
void print_alphabet(void)
{
	char chracter;

	ch = 'a';

	while (character <= 'z')
	{
		_putchar(character);
		ch++;
	}

	_putchar('\n');
}

